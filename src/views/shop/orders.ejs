<%- include('../includes/head.ejs'); %>
    <h1>Orders</h1>
    <% if(!orders?.length){%>
        <h3>You have no orders.</h3>
    <%} else {%>
        <%orders.forEach((order) => {%>
            <h3>Order: <%=order._id%></h3>
            <ul>
                <%order.items.forEach(product => {%>
                    <li><%=product.title%> <br>QTY: <%=product.quantity%></li>
                <%})%>
            </ul>
        <%})%>
    <%}%>
<%- include('../includes/end.ejs'); %>